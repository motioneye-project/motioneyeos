#!/bin/sh

INETD=/usr/sbin/inetd

echo -n "Starting inted:"
if [ ! -x "${INETD}" ]; then
	echo " missing"
	exit 1
fi

if ${INETD} > /dev/null 2> /dev/null; then
	echo " done"
else
	echo " failed"
	exit 1
fi
